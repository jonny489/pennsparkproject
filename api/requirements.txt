fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0
pydantic==2.10.4
pydantic-settings==2.7.0
pyjwt[crypto]==2.10.1
certifi==2024.12.14

# Test-only dependencies. Kept in one file to keep the project easy to set up;
# split into requirements-dev.txt if the dependency list grows.
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
