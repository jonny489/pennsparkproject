# Copy to api/.env and fill in. api/.env is gitignored — never commit real values.

# Supabase Postgres connection string (Project Settings > Database > Connection string).
DATABASE_URL=postgresql://postgres:PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres

# Supabase project URL, e.g. https://<ref>.supabase.co
# Access tokens are ES256-signed; the API verifies them against this project's
# public JWKS endpoint, so no shared secret is needed here.
SUPABASE_URL=

# Browser origins allowed to call this API, comma separated.
CORS_ORIGINS=http://localhost:3000

# LOCAL ONLY. When true the API trusts an X-Dev-User-Id header instead of
# verifying a JWT. Never enable this on a deployed instance.
ALLOW_DEV_USER_HEADER=false
